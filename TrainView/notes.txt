In TransDrawer...
  if (lodOptions.DebugModeManager.IsActive("trains"))

DrawManager.DebugModeManager.ShowView("trains");

DrawManager and DebugModeManager are both in GameSessionOrchestrator.DependancyContainer.

- Changes...
                    var debugmode = SessionDependencyContainer.Resolve<DebugModeManager>();
                    debugmode.ShowView("trains");
- This didn't seem to do anything.
        ActiveViews = (from name in GlobalGamePreferences.GetString("debug-views", "").Split(",")
- There's a config file somewhere?
- Seems to be the settings.json in the game folder.  It persisted "trains" after I set it in the game.

- This looks like a likely candidate...
    public bool DrawTrainWagon(FrameDrawOptionsNoLOD draw, TrainId trainId, TrainData train, int wagonIdx, WagonNavigationData wagonData, out Matrix4x4 wagonTrs)
If traindId matches the currently selected train, and wagonIdx is 0, then use the TRS data to get the position and rotation of the locomotive.

- Issue: If the train is off camera, I still want the camera to snap to it.
- Maybe fallback to find the position using TrainData while the train is off camera?

- Issue: Keypress appears to be repeated a lot.
-- Tested this - seems to be only once.

- Issue: Pressing bind appears to do nothing when trains are close on screen.
- Currently trains only seem to bind when in map mode.  I think that's overview trains.
- Zooming in the camera stops following, but the train is still selected.  Zooming returns the camera to the train.
- Maybe it's not calling the postfix method at all?
- The problem was that the hook was being reused / overwritten.

- Issue: The up-close and off-screen now works. But the up-close on-screen does nothing.
- Because it's not implemented yet.

- Issue: Pressing bind appears to do nothing when trains are off screen in overview mode.
- Looks like these methods are not called when train is off screen.
- The drawer calls TrainIsVisible() before calling DrawTrainWagon().
- How to fix it?
- Maybe use drawer to update the values.  If they don't exist, then fallback using trainData.
- Still need an update() method that is called every frame?

TODO
- Put camera "on" the train.  Need the camera's location, not the camera's target location.
- Rotate the camera to match the train's rotation.  That's the train's yaw? - Done
- Change camera Z distance.
- Fix rotation when train is upside down.
- Ideally the camera would go upside down with the train.

- The issue is that the train drawer skips the DrawTrainWagon() method when the train is off screen.
- What I want is the camera to snap to the train even when it's off screen.
- I think it used to do this, by using the trainData.
- I have that, but the postfix method is not being called.
- So I need some update method that is called.  MyGameObject has that.
- I could have the train render postfix cache the TRS data for the currently bound train.  And not change the camera.
- Then in the gameObject update(), see if that data exists and use it.  Otherwise fallback to the trainData method.
- I don't know what order these methods are called in.
- Is it safe to delete it, or mark it as used/dirty?
- I don't even know if they are on the same thread.  Maybe I can check that?
- Better to use a different update method hook.  One that I know is synced with the train drawer.
- prefix and postfix for the drawer update?  DoDraw() - I assume it's called every update.

- Question: There are some cases where the wagonTrs is computed, but the wagon is not drawn.  Off camera I guess.  Should Train View use the TRS?

Issue: When the train is off screen (but drawn?) the camera snaps to 0,0.
Issue: Looks like it is always using the estimated location when snapping to the train.
- Made a change to reverese the logic for wasDrawn.
Issue: Now it is worse.  Train 3 overview seems to work, but trains 1 and 2 snap to zero in overview.  Zooming in, the camera is "torn" between two positions.
- Fix: The TRS data was being initialized for every train wagon.

- ViewportHeightEasingAnimationController appears to be where height is controlled.
- CameraController has this...
        Parent.position = new float3((float)Viewport.Position.x, height, (float)Viewport.Position.y);
- I don't know if changing the parent (camera?) or the global height is the correct way to do it.

        MainCamera = Viewport.MainCamera;
        Parent = MainCamera.transform.parent;
- rotation example...
        wagonTrs = Matrix4x4.TRS(worldPos + new WorldVector(0f, 0f, heightOffset), Quaternion.Euler(FastMath.NormalizeAngleDegrees(roll + wagonRenderData.CurrentLeanAngle), yaw, pitch), scale);
